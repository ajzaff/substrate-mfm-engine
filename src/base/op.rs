#[repr(u8)]
#[derive(Copy, Clone, Debug)]
pub enum MetaOp {
    Name,
    Symbol,
    Desc,
    Author,
    License,
    Radius,
    BgColor,
    FgColor,
    Symmetries,
    Field,
    Parameter,
}

#[repr(u8)]
#[derive(Copy, Clone, Debug)]
pub enum Op {
    Nop,
    Exit,
    Copy,
    Swap,
    Scan,
    UseSymmetries,
    RestoreSymmetries,
    Push,
    Pop,
    Call,
    Ret,
    Checksum,
    Add,
    Sub,
    Neg,
    Mod,
    Mul,
    Div,
    Less,
    LessEqual,
    Or,
    And,
    Xor,
    Equal,
    BitCount,
    BitScanForward,
    BitScanReverse,
    LShift,
    RShift,
    Jump,
    JumpRelativeOffset,
    JumpZero,
    JumpNonZero,
    LookupSite,
    LookupRegister,
}
